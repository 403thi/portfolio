export default interface TitleProps {
    children : any
    size     ?: 'small' | 'medium' | 'big' | 'extra'
    outlined ?: boolean
    blur     ?: boolean
    kana     ?: boolean
    vertical ?: boolean
}