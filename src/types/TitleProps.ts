export default interface TitleProps {
    children : any
    size     ?: 'extra-small' | 'small' | 'medium' | 'big' | 'extra'
    outlined ?: boolean
    kana     ?: boolean
    vertical ?: boolean
    monospace?: boolean
}